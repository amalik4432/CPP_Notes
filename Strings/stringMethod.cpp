#include <iostream>
#include <string>
using namespace std;

int main()
{
    string s1, s2, str;
    str = "Bakchodi";
    s1 = "fam";
    s2 = "ily";
    // str = s1.append(s2); // combine
    // cout << str << endl;

    //  str = s1 + s2;
    // cout << str << endl; // s1 and s2 remain seperate.

    // s1.clear(); clear  the string s1.

    // s1.compare(s2); // compare if its grater or smaller or equal.

    // s1.clear(); clear  the string s1.
    // if (s1.empty()){
    //     cout<<"String is empty"
    // }

    // str.erase(3 /* starting index*/, 2 /* number of characters to be deleted from starting index */); // delete some part of string.

    // str.find("kc")<<endl; //used to find in string.

    // str.insert(8, " mat kar"); // insert at a particular position.
    // cout << str << endl;

    // int n = str.size(); /* gives the size of the string */
    // int n = str.length(); /* gives the length of the string*/

    // int n = s1.substr(3,5).length(); // it will give the number of characters from index 3 to 8.

    // string s3 = "880";
    // int x = stoi(s3); // convert string into integer.

    // int x = 770;
    // string y = to_string(x); // convert integer into string.
    // cout << y << endl;
    // cout << y + "2"  << endl;

    return 0;
}
